export const sumar = ( a, b ) => {
    return a + b;
}

export const multi = ( a, b ) => {
    return a * b;
}

export const restar = ( a, b ) => {
    return a - b;
}

export const div = ( a, b ) => {
    return a / b;
}
